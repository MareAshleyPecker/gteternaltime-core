package rain.gtetcore.gtet.common.material

import rain.gtetcore.gtet.common.*
import com.gregtechceu.gtceu.common.data.materials.*

object ETElementMaterials {
    /**
     * 初始化在register里，到总线[CommonProxy.kotlinInit]里注册
     *
     * 注册请参考 [ElementMaterials]
     *
     * example val NAME = Material.Builder(GTCEu.id("[ETMaterial].NAME")).function
     *
     * */
    fun register() {}
}